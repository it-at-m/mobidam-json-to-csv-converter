[project]
name = "json-converter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8.7"
dependencies = [
    "flatten-json>=0.1.14",
    "pandas>=2.0.3",
]
